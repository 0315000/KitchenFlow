# KitchenFlow

1. `git clone https://github.com/0315000/KitchenFlow.git` 로 클론
2. 백엔드 실행: `src/KitchenFlow.Api` 폴더에서 `dotnet run`
3. 프론트엔드 실행: `src/KitchenFlow.Api/ClientApp` 폴더에서 `npm run dev` (이후 브라우저에서 `http://localhost:5173` 접속하면 기계 목록이 보임)

## test
